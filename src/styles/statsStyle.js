import {
  StyleSheet,
  Image
} from 'react-native';

export const statsStyle = StyleSheet.create({
  container: {
    padding: 5,
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around'
  },
  stat: {
    backgroundColor: 'transparent',
    margin: 10,
    alignItems: 'center'
  }
});
